module.exports = function(config) { // 传入 config 为默认 webpack 配置
  // 此处可对配置做定制化修改
  console.log("dingding 正在启动，请稍后 .... zhaoxudong...");
//  console.log("config..." + JSON.stringify(config));
//  console.log("entry old..." + JSON.stringify(config.entry));
//  console.log("entry new..." + JSON.stringify(config.entry));
//  config.entry.config = "./src\\api\\config.js";
//  config.plugins.push(new webpack.optimize.CommonsChunkPlugin({
//      name:'config', // 上面入口定义的节点组
//      filename:'config.js' //最后生成的文件名
//  }));
 // config.plugins.push(new SomeWebpackPlugin());
};